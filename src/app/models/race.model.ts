// Pour représenter une course,
// nous allons créer une interface TypeScript dans un nouveau dossier models (dans app).
// Cette interface sera dans un fichier nommé race.model.ts
// et contiendra la déclaration d’une entité nommée RaceModel avec un seul champ name de type string.
export interface RaceModel {
  name: string;
}

