/* L’interface PonyModel contient quant à elle un champ id de type number et deux champs de type string : name et color. */

export interface PonyModel {
  id: number;
  name: string;
  color: string;
}
